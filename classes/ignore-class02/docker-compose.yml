version: '3.3'

services:
  class-02-services:
    build: .
    image: scl-class-02-services  # name of the built image
    container_name: scl-class-02-services
    stop_grace_period: 0s
    networks:
      playground-net:
        ipv4_address: 172.20.0.88

networks:
  playground-net:
    external: true